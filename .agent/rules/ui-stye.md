---
trigger: manual
---

# UI Design Style Analysis: Modern Minimal Luxury (Final Brief)

## 1. ชื่อสไตล์ (Style Definition)
สไตล์นี้เรียกว่า **"Modern Minimal Luxury"** หรือ **"Clean Minimalist with Elegant Touch"** เป็นการผสมผสานระหว่างความเรียบง่ายทันสมัย (Minimalism) เข้ากับกลิ่นอายความหรูหราคลาสสิก (Luxury) เน้นความสะอาดตา การจัดวางที่มีระเบียบ และการใช้พื้นที่ว่างเพื่อขับเน้นให้สินค้าดูมีมูลค่า

---

## 2. กฎระบบการออกแบบและขอบเขตงาน (Design System & Scope Rules) ⚠️ **CRITICAL**

เพื่อให้งานออกแบบเป็นไปในทิศทางเดียวกันและง่ายต่อการพัฒนาต่อ ต้องปฏิบัติตามกฎ 3 ข้อนี้อย่างเคร่งครัด:

### 🔐 Rule 1: No Hard-Coding (ใช้ Design Tokens เท่านั้น)
ห้ามใช้ค่า Hex Code หรือชื่อ Font โดยตรงในหน้าจอออกแบบ (Screens) **ต้องเรียกใช้ผ่าน Token Variable เท่านั้น**
* **Color Tokens:** ห้ามใช้ `#FFFFFF` หรือ `#008080` ให้ใช้ตัวแปรเช่น `$color-bg-primary`, `$color-brand-accent`
* **Typography Tokens:** ห้ามกำหนด Size/Weight เอง ให้ใช้ Token เช่น `$heading-xl-serif`, `$body-m-sans`

### 🧩 Rule 2: Component First Workflow (Reuse before Create)
การทำงานต้องยึดหลัก **"ใช้ของเดิมที่มีก่อนสร้างใหม่"**:
1.  **Check Library:** ก่อนเริ่มออกแบบหน้าใหม่ ให้เช็คในไฟล์ `design-system` เสมอ
2.  **Use Instances:** ดึง Component พื้นฐาน (Buttons, Inputs, Dialogs, Cards, Navigation) มาจาก Library เท่านั้น
3.  **New Component:** อนุญาตให้สร้างใหม่ได้เฉพาะเมื่อ **"ไม่มีในระบบ"** จริงๆ และเมื่อสร้างเสร็จต้องนำกลับไปอัปเดตลงใน Design System ด้วย

### 🚧 Rule 3: Visual Mockup Only (No Real Functionality)
ในเฟสนี้เน้นทำ **Static High-Fidelity Mockup** เท่านั้น:
* **Visual Focus:** โฟกัสเรื่องความสวยงาม, การจัดวาง (Layout), และการใช้ Token ให้ถูกต้องเป๊ะๆ
* **No Logic:** **ไม่ต้องใส่ฟังก์ชันการทำงานจริง** ไม่ต้องเขียน Script คำนวณ หรือเชื่อมต่อ Database
* **Dummy Data:** ข้อมูลในหน้าจอ (ชื่อ, ราคา, รูปภาพ) ให้ใช้ข้อมูลสมมติ (Dummy Content) ที่ดูสมจริงใส่เข้าไปได้เลย ไม่ต้องดึงจากระบบจริง

---

## 3. คำอธิบายเชิงภาพและ Token Mapping (Visual Breakdown)

### 🌟 บรรยากาศและอารมณ์ (Mood & Tone)
* **ความรู้สึก:** โปร่ง โล่ง สบายตา (Airy) เชื่อถือได้และดูแพง (Trusted & High-end)
* **จินตนาการ:** ล็อบบี้โรงแรม 5 ดาว หรือเคาน์เตอร์แบรนด์เนม โทนสีขาวสว่าง

### 🎨 ชุดสี (Color Palette & Tokens)
* **Background:** สีขาวสะอาด
    * *Token:* `$color-surface-base` / `$color-bg-primary`
* **Hero Color:** เขียวทิฟฟานี่เข้ม (Deep Teal/Mint) สำหรับปุ่มและจุดเน้น
    * *Token:* `$color-action-primary` / `$color-brand-accent`
* **Text Color:** เทาเข้มเกือบดำ (Dark Charcoal)
    * *Token:* `$color-text-main` (Headings), `$color-text-secondary` (Body)

### ✍️ ตัวอักษร (Typography & Tokens)
* **Headings:** ฟอนต์ Serif (มีเชิง) หรูหรา เช่น *Playfair Display*
    * *Token:* `$font-family-serif` | *Styles:* `$text-display-lg`, `$text-heading-h1`
* **Body Text:** ฟอนต์ Sans-serif (ไม่มีเชิง) อ่านง่าย เช่น *Roboto*
    * *Token:* `$font-family-sans` | *Styles:* `$text-body-md`, `$text-caption`

### 🔶 รูปทรงและองค์ประกอบ (Shapes & Components)
* **Buttons & Inputs:** สี่เหลี่ยมผืนผ้า **มุมมนเล็กน้อย**
    * *Token:* `$border-radius-sm` (approx 4px-6px)
    * *Component:* `Btn-Primary`, `Input-Text-Standard` (ดึงจาก Library)
* **Decoration:** Geometric Abstract
    * *Asset:* ใช้ SVG Asset จาก Library หมวด `Decoration-Graphics`

### 📐 การจัดวาง (Layout & Grid)
* **Split Screen:** แบ่งหน้าจอซ้าย-ขวา (เช่น หน้า Login)
    * *System:* ใช้ Grid System 12 Columns
* **Whitespace:**
    * *Token:* `$spacing-xl`, `$spacing-xxl` (ใช้ค่า Token แทนการกะระยะ)

### 💡 แสงและเงา (Depth & Shadow)
* **Soft Shadow:** เงาฟุ้งบางๆ
    * *Token:* `$shadow-elevation-low` (Cards), `$shadow-elevation-medium` (Floating Elements)

---

## 4. สรุปคีย์เวิร์ดสำหรับบรีฟงาน (Key Takeaways for Briefing)

หากต้องการส่งต่อให้ทีมงาน ให้ใช้ Checklist นี้:

1.  **Scope:** **"Static Mockup Only"** - No code logic needed yet. Just visual design with dummy data.
2.  **Strict Design System:** "Check existing components first. Use Library instances (Button, Dialog, etc.)."
3.  **Tokenization:** "No hard-coded colors/fonts. Map everything to Tokens."
4.  **Style:** Modern Minimal Luxury, White dominant, Deep Teal accent.
5.  **Font:** Serif Headers + Sans-serif Body.